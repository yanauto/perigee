# playbooks · 可复跑剧本

逐步操作手册（部署、回灌镜像、装机、联调验收…）。

## 何时新建

本场**首次打通或修正**一条路径，且步骤超过「地图上的一行命令」→ 新建 `docs/playbooks/<动作>.md`，并在 `docs/代码地图.md` 操作入口表加一行链接。

## 建议结构

```markdown
# 标题
## 前置条件
## 步骤（可复制命令）
## 验收
## 回滚 / 失败时
## 密钥指针（无正文）
```

## 禁止

- 密钥/token 正文  
- 把进度故事写进剧本  
- 与工程手册重复抄三遍接口表（剧本只引用手册）
